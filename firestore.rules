rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules allow anyone to read and write to your database.
    // This is for testing only - change before production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}